[c]
label = "clipboard"

[c.d]
label = "copy current directory"
run = "pwd | tr -d '\\n' | $MENYY --copy"

[c.p]
label = "copy current directory path (absolute)"
run = "realpath . | tr -d '\\n' | $MENYY --copy"

[q]
label = "quit"
run = "true"
